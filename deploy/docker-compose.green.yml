version: '3.7'

services:
  tictoc-green:
    image: mungzogi/tictoc-api:latest
    container_name: tictoc-green
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    env_file:
      - .env
    ports:
      - "8082:8080"
    networks:
      - tic-toc-net

networks:
  tic-toc-net:
    name: tic-toc-net
    external: true